/**
 * Endereço do código aberto, num lugar só.
 *
 * O link aparece no cabeçalho da tela e no aviso do modo publicado. Repetir a
 * string em dois arquivos é como um deles acaba apontando para o repositório
 * antigo depois de um rename — e ninguém clica num link do rodapé para
 * descobrir que quebrou.
 */
export const REPO_URL = "https://github.com/andrebassi/retrieval";

/** Sem `https://`: é o que cabe num chip e é o que a pessoa digita. */
export const REPO_LABEL = "github.com/andrebassi/retrieval";
